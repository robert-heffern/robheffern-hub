# robheffern-hub

The cabinet-of-curiosities hub at robheffern.palaich.net.

Static site built with Astro. Cloudflare Pages builds and hosts it.
- Build command: `npm run build`
- Output directory: `dist`

To add or edit a room, edit files under `src/pages/`. Push to this repo and the live site rebuilds automatically.
